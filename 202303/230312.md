
```swift
func sayHello() {
    print("Hello")
}

let f1 = sayHello //형식 추론 방식
f1()

func printHello(with name: String) {
    print("hello \(name)")
}
let f2: (String) -> () = printHello(with:) //함수 표기

f2("World")  // hello World
상수에 저장된 함수 호출시에 arugment label 사용 안한다.


func add(a: Int, b: Int) -> Int {
    return a + b
}

var f4: (Int, Int) -> Int = add(a:b:)
f4(1, 3)
```
